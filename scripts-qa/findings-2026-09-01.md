# Findings inventory — audit session 2026-09-01

## Confirmed findings

### F1 [a11y, minor] Walled pages have no h1
- PlanGate's UpgradeWall renders `<h3>{feature}</h3>` (src/components/UpgradeWall.tsx:62).
- /ai-coach, /seo, /ai-humanizer for a free user render with zero h1 in the document
  (PageHeader's h1 is inside gated children). Loading state (spinner) also has no heading.
- Fix: UpgradeWall h3 → h1.

### F2 [functional/privacy, medium] Shared score cannot be revoked from the UI
- DELETE /api/share/[id] (src/app/api/share/[id]/route.ts:154) is fully implemented
  ("immediately un-publishes the page, badge, OG image and community link").
- ShareScoreButton.tsx:14 documents "the creator can revoke from the score card's own
  controls (which DELETEs the stamp)" — but NO component in src/ calls DELETE on /api/share.
- Verified: analysis page has Share score button only; reports page has Share/Open only;
  DataPrivacyPanel has no share controls. A creator who shares cannot un-share via UI.
- Fix: add Unshare control where the report is shared: analysis page (ShareScoreButton)
  and/or reports list row (ReportActions) — swap Share→Unshare once sharedAt exists.

### F3 [security, CRITICAL] Live Clerk sign-in token still in git HEAD
- `scripts-qa/signin-token.jwt` (exp 2026-09-13 — still valid) is tracked in HEAD
  (commit d754d11) of PUBLIC repo github.com/muneebi-85/publish-saas-app.
- Deletions are STAGED but uncommitted; history purge + token revocation pending.
- Also: session.jwt, session-long.jwt, client-session.jwt, clerk-cookies.json,
  .chrome-profile/** (incl. Login Data vault), probe.mp4 in history.
- Action: needs git commit + filter-repo purge + Clerk-side revocation. User action
  required for push/rotate; I can prepare the commit.

### F4 [security, HIGH] next 14.2.15 — CVE-2025-29927 middleware auth bypass
- x-middleware-subrequest header bypasses middleware; no mitigation present.
- Fix: bump next to ^14.2.35 (patch-line drop-in). Also npm audit fix transitive
  (nanoid, js-cookie, @clerk/clerk-react advisories).

### F5 [ops, HIGH] Vercel never runs `prisma migrate deploy`
- build script = `prisma generate && next build`; db:deploy manual-only. Prior
  incident documented in migration 20260830 (P2021/P2022 crashes from drift).
- Fix: add migrate deploy to Vercel build command (vercel.json or dashboard).

### F6 [compliance, HIGH] Newsletter unsubscribe never written
- `unsubscribedAt` only ever set to null (resubscribe); no unsubscribe path
  anywhere; email.ts sends marketing mail. GDPR/CAN-SPAM gap.

### F7 [bug, MEDIUM] OG image origin falls back to http://localhost:3000
- src/app/share/[id]/opengraph-image.tsx:37 — ignores env.ts's VERCEL_URL
  derivation; broken social previews on Vercel-URL-only deployments.

### F8 [dead code, MEDIUM] Project/MediaAsset/Comment models never written
- /api/account/export exports always-empty sections for them.

### F9 [reliability, MEDIUM] hasJobQueue() gates on QSTASH_TOKEN only
- Worker 503s without signing keys; jobs stranded until cron reconcile refunds.
- env.ts:159 — should require token + both signing keys.

### F10 [ops, MEDIUM] CRON_SECRET unset = cron routes silently never run
- Billing reconcile + GDPR purge never execute; only boot-time console.warn.
- (Fail-closed is correct; visibility is the gap.)

### F11 [hygiene, LOW] Unused deps: framer-motion, @tanstack/react-query,
  zustand, @ai-sdk/openai, ai, tsx. research-2026/ untracked & not ignored.
  SENTRY_DSN documented but unread. No engines field in package.json.
  robots.ts disallows nonexistent /inbox.


- All 31 routes render, no app errors, no meaningful console errors
- /upload quota-wall: 402 → redirect to /pricing (intended)
- Full analyze journey: fill form → POST → report page renders (score 80, all layers)
- Free window rolls auditsUsed when periodStart is >30d old (lazily, verified via /api/me/plan)
- Share: POST stamps sharedAt (idempotent), /share/[id] public page renders score/title/platform only
- Badge /api/badge/[id]: SVG 200
- X/LinkedIn intent URLs build correctly; clipboard copy feedback works
- Cookie banner: accept → localStorage persisted, banner gone
- 404 page: friendly "Nothing to review here" with 404 status

### F12 [race/data-loss, HIGH] Challenge re-run prefill clobbers user edits
- MultiAssetUploader.tsx:164-203 — `?title`/`?script` prefill effect sets state
  unconditionally; challenge fetch (~async) later overwrites with
  setTitle/setScriptText. User's pasted edits lost silently.
- Fix: only set state if the field is still untouched (track a dirty flag).

### F13 [perf/consistency, MEDIUM] Sidebar double-mounted (desktop + MobileNav drawer)
- Two Sidebar instances in tree → duplicate /api/me/plan + /api/notifications
  fetches; independent state can disagree across instances.

### F14 [logic/UX, HIGH] Reports trend: Sparkline trend color vs delta badge use
  different formulas and contradict each other
- page.tsx:96-130 delta = last - secondToLast; Sparkline.tsx:45 trend color =
  data[len-1] - data[0]. With 3+ scores the arrows disagree in one cell.
- Fix: make both use the same (last vs first-of-window? or last vs prev) formula.

### F15 [crash-path, MEDIUM-HIGH] ShareScoreButton clipboard fallback unprotected
- ShareScoreButton.tsx:29-43 — execCommand path not try-wrapped (ReportActions'
  equivalent IS wrapped). Failure => report already public, nothing copied,
  zero feedback. Fix: wrap in try/catch like ReportActions.

### F16 [race, MEDIUM] useConnectChannel OAuth return effect: mount-only closure
  captures pre-hydration state; 401 => ?connect param stripped, auto-finish lost.

### F17 [nav, MEDIUM] Settings community card unreachable by ?tab= deep link
- SECTIONS lacks 'community' though card id="community" renders (SettingsClient:604).

### F18 [hydration, LOW-MEDIUM] toLocaleDateString without timeZone in
  AnalysesClient (155/160) and ProjectsClient:269 — server/client date can differ
  across day boundaries (ChannelsClient already fixed with timeZone:'UTC').

### F19 [latent, LOW] ReferralPanel.tsx:46 reads window.location.origin during
  render — safe today only because status is null on server pass; a refactor
  that seeds status crashes SSR. Also copies preview-host links.

### F20 [resilience, LOW] ChallengeCompare: failed accept POST replaces the whole
  comparison panel with error box, no retry; verdict unrenderable.


- (browser journey continues: projects CRUD, settings, brand-kit, templates,
  notifications, help, community, sign-in/up logged-out, responsive, keyboard nav;
  then pro-user flows: ai-coach, seo studio, humanizer, channel pages)
- 4 static audit agents running (API, frontend, lib, config)
## Verified-clean areas (browser + agent reports)
- Browser: 31 routes render; analyze journey end-to-end; share publish/public page/badge;
  cookie banner; 404; quota wall redirect; X/LinkedIn intents; clipboard feedback.
- Agent (config): migrations match schema (9 migrations, no drift); ML parity TS/Python
  identical (884 cases); billing/Clerk/QStash/cron auth all fail-closed; middleware
  matcher + safeRedirect correct; next.config image hosts scoped; schema onDelete
  cascades correct; env vars all documented.

### F21 [billing, HIGH] Paused subscription escapes all 3 erasure-path cancels
- BILLABLE sets in account/delete, clerk webhook, purge cron omit 'paused'
  (checkout route knows paused bills). Fixed all three + comments.

### F22 [correctness, HIGH] Copyright LLM path lacks heuristic floor
- copyright-engine.ts: LLM may return musicMatchRisk Low for 'popular'
  commercial track (96/100) while fallback grades High (45). Voice engine
  has the floor guard; copyright doesn't.

### F23 [security, MEDIUM] Billing webhook email fallback grants to newest account
- webhook/route.ts:94-104 — custom_data.user_id absent => paid plan attaches
  to newest row with that email (unverified).

### F24 [billing, MEDIUM] Missed subscription_payment_success => auditsUsed
  never resets; customer 402-blocked all month. extend-period writes periodEnd
  only. Same hole in subscription_updated same-tier branch.

### F25 [ML parity, MEDIUM] TS parseTime/wallClock accept what Python rejects
  (non-ISO fallback, 2026-02-31 roll-over in wallClock). publishedAt unvalidated
  at publish-score route. => feature drift.

### F26 [billing, MEDIUM] subscription_cancelled with no row updateMany 0 =>
  ACK 200, never retried; later created writes status active + cancelledAt null.

### F27 [lib-agent channel squat MEDIUM] public-link channel connect claims
  any public channel URL; real owner then 409-blocked forever.
  (channels/route.ts public-link path — no ownership verification.)

### F28 [low] ratelimit.ts trusts Upstash pipeline result shape (error object
  => count NaN => permanent 429s); SSRF guard misses 0x/decimal IP encodings;
  tag_avg_words whitespace-class drift vs Python; publish-score durationSeconds
  raw Number() coercion; subscription_payment_success on unmapped variant
  resets a free user's counter; checkout lacks idempotency key (double-tab
  double-bill); jsonBody buffers before size cap; presign lacks aggregate
  storage ceiling.
