# ─── Dependencies ───
node_modules/
.pnp/
.pnp.js
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ─── Testing ───
coverage/

# ─── Next.js ───
.next/
out/
build/
dist/

# ─── Production ───
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ─── Environment (NEVER COMMIT) ───
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# ─── IDE / OS ───
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# ─── Deployment ───
.vercel
.netlify

# ─── Prisma ───
prisma/*.db
prisma/*.db-journal
prisma/migrations/dev

# ─── TypeScript ───
*.tsbuildinfo
next-env.d.ts

# ─── Uploads / temp ───
uploads/
tmp/
.gstack/

# ─── Design measurement scratch (local machine paths) ───
.design-ref/
# Stitch comp + reference renders used to build the landing page. Local only:
# the spec lives in the design tool, and the PNGs are multi-MB screenshots.
.stitch/
.next-dev.log
.next-prod.log
.next-build.log

# Training data and model artefacts. Hundreds of MB of thumbnails and JSONL;
# regenerate with `python -m publishml.collect` rather than committing it.
ml/data/
ml/data-smoke/
ml/**/__pycache__/
.tmp-*.json
.fs-probe.txt
